# Aegis SRE — local (on-prem) runtime dependencies.
# Cloud-tier extras (Redis, Postgres) live in requirements-cloud.txt.
# Install:  pip install -r requirements.txt

# --- API / web ---
fastapi>=0.110
uvicorn[standard]>=0.29          # ASGI server (bundles websockets for /ws)

# --- Core / config / models ---
pydantic>=2.5
structlog>=24.1
openai>=1.40                     # OpenAI-compatible LLM client (local Ollama at /v1)
python-dotenv>=1.0               # load a local .env on startup

# --- Orchestration (LangGraph repair swarm + checkpointer) ---
langgraph>=0.2
langgraph-checkpoint-sqlite>=2.0 # AsyncSqliteSaver checkpointer
aiosqlite>=0.20                  # async SQLite backing the checkpointer

# --- RAG (local Ollama embeddings + Chroma) ---
llama-index-core>=0.11
llama-index-vector-stores-chroma>=0.2
llama-index-embeddings-ollama>=0.3
chromadb>=0.5

# --- Telemetry sources / integrations ---
kubernetes>=29.0                 # K8s CrashLoopBackOff watcher
PyGithub>=2.3                    # GitHub PR provider

# --- Observability (optional; metrics no-op if absent) ---
prometheus-client>=0.20

# --- Tests ---
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27                      # FastAPI TestClient

# --- Optional sandbox backend (only if SANDBOX_PROVIDER=e2b) ---
# e2b>=0.17
