# Extra dependencies for the cloud (scaled) profile only.
# On-prem needs none of these — its backends are stdlib (sqlite3) + in-memory.
redis>=5.0          # RedisCache (atomic SET NX EX) + RedisStreamBroker
asyncpg>=0.29       # PostgresEventStore connection pool
prometheus-client>=0.20  # /metrics endpoint (optional; no-op if absent, on either tier)
opentelemetry-sdk>=1.20             # tracing (optional; no-op if absent)
opentelemetry-exporter-otlp-proto-http>=1.20  # OTLP span export (set OTEL_EXPORTER_OTLP_ENDPOINT)
